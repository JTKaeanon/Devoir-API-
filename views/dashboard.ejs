<!DOCTYPE html>
<html lang="fr">

<head>
    <%- include('partials/head') %>
</head>

<body>
    <div class="dashboard-container">

        <%- include('partials/sidebar') %>

            <main class="main-content">
                <header class="header">
                    <div>
                        <h1>Tableau de bord</h1>
                        <p class="date-du-jour">Date du jour : <%= new Date().toLocaleDateString('fr-FR') %>
                        </p>
                    </div>
                    <div class="user-info">
                        <% if (user) { %>
                            👤 <%= user %>
                                <% } else { %>
                                    👤 Non connecté
                                    <% } %>
                    </div>
                </header>

                <div class="card">
                    <h3>Bienvenue</h3>
                    <p>Sélectionnez une action dans le menu de gauche pour gérer le port.</p>
                </div>
            </main>
    </div>
</body>

</html>